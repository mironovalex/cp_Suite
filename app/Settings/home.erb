<div data-role="page">

  <div data-role="header" data-position="inline">
    <h1>В начало</h1>
    <% if SyncEngine::logged_in > 0 %>
        <a href="<%= url_for :action => :do_sync %>" class="ui-btn-left" data-icon="refresh">Синхр.</a>
        <a href="<%= url_for :action => :logout %>" class="ui-btn-right" data-icon="star">Выход</a>
    <% else %>
        <a href="<%= url_for :action => :login %>" class="ui-btn-right" data-icon="star">Вход</a>
    <% end %>
  </div>

  <div data-role="content">
    <ul data-role="listview">
      <li><a href="#"></a></li>
    </ul>
  </div>

</div>
